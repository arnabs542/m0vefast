def minWindowSubsequence(self, S: str, T: str) -> str:
    '''
    Given strings S and T, find the minimum (contiguous) substring W of S, so that T is a subsequence of W.
    Input:
    S = "abcdebdde", T = "bde"
    Output: "bcde"
'''
